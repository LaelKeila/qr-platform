<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Confirmation</title>
    <style>
        body {
            font-family: 'Segoe UI', sans-serif;
            background: linear-gradient(to right, #FFB84D, #FF6600); /* Mélange orange et jaune */
            color: white;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding-top: 60px;
        }
        .container {
            background: rgba(255, 255, 255, 0.1);
            padding: 30px;
            border-radius: 15px;
            width: 90%;
            max-width: 400px;
            text-align: center;
            box-shadow: 0 8px 16px rgba(0,0,0,0.2);
        }
        img {
            margin-top: 20px;
            width: 250px;
            height: 250px;
        }
        h2 {
            margin-top: 0;
        }
        .download-button {
            background-color: #FF6600;
            padding: 10px 20px;
            color: white;
            text-decoration: none;
            border-radius: 8px;
            margin-top: 20px;
            display: inline-block;
        }
        .download-button:hover {
            background-color: #FFB84D;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Merci {{ name }} !</h2>
        
        {% if qr_code %}
            <p>Voici ton QR code d’accès à l’événement 🎟️</p>
            <img src="{{ qr_code }}" alt="QR Code">
            <p>Garde-le précieusement, il sera scanné à l’entrée.</p>

            <!-- Bouton pour télécharger le QR code -->
            <a href="{{ url_for('download', filename=qr_filename) }}" class="download-button">Télécharger ton QR Code</a>
        {% else %}
            <p>{{ message }}</p>
        {% endif %}
    </div>
</body>
</html>
